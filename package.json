{
  "name": "masalaandmug-backend-demo",
  "version": "0.1.0",
  "private": true,
  "description": "Demo lightweight backend for coupon validation and save-for-later persistence",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "jest --ci --reporters=default --reporters=jest-junit"
  },
  "dependencies": {
    "express": "^4.18.2",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "supertest": "^6.3.0",
    "jest-junit": "^13.0.0"
  },
  "jest-junit": {
    "outputDirectory": "test-results",
    "outputName": "junit.xml"
  }
}
